# ─────────────────────────────────────────────────────────────────────────────
# YONNE — Variables d'environnement de PRODUCTION
# Ce fichier est commité dans le dépôt : il ne doit contenir AUCUN SECRET.
# Les valeurs sensibles (clés API, JWT secret) sont injectées exclusivement
# via le Dashboard Vercel → Project Settings → Environment Variables.
# ─────────────────────────────────────────────────────────────────────────────

# Flag applicatif — active tous les guards IS_PROD (simulation off, LiveFeed réel)
NEXT_PUBLIC_APP_ENV=production
